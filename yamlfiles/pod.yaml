apiVersion: v1
kind: Pod
metadata:
  name: flask-app
  labels:
    app: flask-app
spec:
  containers:
    - name: flask-app 
      image: msaldivar/flask-app:latest
      imagePullPolicy: Always
      ports:
        - name: http
          containerPort: 5000
          protocol: TCP
